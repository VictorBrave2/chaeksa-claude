# 궁극의 책사 — 작업 규약

이 파일은 **매 세션 자동으로 로드된다**(서브에이전트에도 간다).
그래서 짧다. 전문은 `docs/`에 있고, 여기는 **선(線)과 길잡이**만 둔다.

`docs/`는 자동으로 안 온다. **아래 길잡이 표대로 열어라** — 안 열고 지나가면 조문을 어긴다.
실제로 그런 적이 있다(안 잰 온도를 단정한 발언 하나가 라이브에 나갔다).

---

## 하나 · 어겨서는 안 되는 선

### 판정

- **잰 것만 말한다.** 계산에 없는 연도·글자·숫자·삶의 장면을 지어내지 않는다.
  「값이 없다」와 「조용하다」는 다르다 — 계산이 안 나온 것을 결론으로 부르지 마라.
- **아첨 금지.** 잰 것을 좋게 바꿔 적는 것이 이 집에서 가장 큰 잘못이다.
  **냉정한 것은 계산이고 따뜻한 것은 말투다.** 말투를 따뜻하게 하되 판정을 따뜻하게 만들지 마라.
- **겁주기 금지.** 삼재·대흉으로 불안을 팔지 않는다. 나쁜 자리도 그대로 재되 협박하지 않는다.
- **기준을 공개한다.** 이것이 파는 물건이다. 판정을 감추면 파는 물건이 사라진다.
  엔진을 고쳐 발행물 순위가 바뀌면 숨기지 말고 밝힌다.

### 돈

- **LLM 을 부르는 곳은 `app/ai.js` 하나, 그 문은 `app.js 간명예열()` 하나뿐이다.**
  입구를 더 만들면 자물쇠 밖에서 겹쳐 굽는다. 굽기를 만지기 전에 `[[project-chaeksa-status]]`
  「원가 자물쇠」를 읽어라.
- **무료 의논은 LLM 을 안 쓴다**(`app/chaeksadan.js` 규칙 조립기, 원가 0).
  여기에 LLM 을 끌어들이면 전략의 뼈대가 부러진다.
- 시간초과는 **자동 재시도 금지**. 이미 쓴 토큰이다.

### 비밀

- **`service_role` 키는 어디에도 쓰지 않는다.** 등급 승격이 막혀도 한도로 버틴다.
- **비밀을 저장소에 커밋하지 않는다.** 마이그레이션 SQL 에 키를 적어 넣지 마라
  (한 번 그럴 뻔했다). 값은 대화로만 전한다.
- `.openai_key` 는 로컬 전용이고 내용을 출력하지 않는다.

---

## 둘 · 손대기 전에 열 것 (길잡이)

| 만지는 것 | 반드시 열 문서 |
|---|---|
| 명리 판정·통변 문장 | `docs/19_법전.md` |
| 엔진의 표·상수·산식 | `docs/15_동결표.md` (948줄. 해당 절만 열어도 된다) |
| 격국 성패 | `docs/16_자평진전_성패.md` |
| 조후·온도 | `docs/12_조후용신표.md` |
| 재물 그릇 | `docs/14_재물그릇_산식.md` |
| 재·관을 삶으로 읽는 법 | `docs/27_영역관제.md` (**궁극의 관법**. 貴賤 폐기 · 官은 영역이다) |
| 영역을 무엇으로 말할까 | `docs/28_영역전개_전략.md` (열넷을 늘어놓고 엔진과 맞춘 표 · 우선순위) |
| 인연·궁합 | `docs/18_인연법.md` · `docs/23_도화홍염_원문.md` |
| 주변인(어머니·친구·상사·시댁·자녀) | `docs/30_주변인.md` (육친표·궁위표는 **결재 뒤에** 구현. 없는 것은 「안 나왔다」다) |
| 화면 말·순서·연출 | `docs/21_공주님원칙.md` |
| 홈·화면 구조·무엇을 먼저 짓나 | `docs/29_비서.md` (**서고에서 비서로.** 오늘 한 사람이 한 문단 · 시간순 · 달력 · 기억) |
| 화자·책사 캐릭터 | `docs/22_책사단.md` |
| 새 화면·새 문장의 값어치 | `docs/24_왜_보는가.md` (니즈 다섯 층) |
| 무엇을 먼저 할 것인가 | `docs/25_전략.md` |
| 블로그·자료 형식 | `docs/10_두형식.md` |
| 결제 | `docs/17_결제.md` |

**부딪히면 법전(19)이 이긴다.** 공주님 원칙(21)은 **전달**을 지배하지 판정을 지배하지 않는다.

죽은 문서 둘 — `docs/00_현재상태.md`(v148 사본, 300판 낡음) ·
`docs/20_협기변방서.md`(엔진에서 들어냄, 채록 기록으로만 남김). 근거로 쓰지 마라.

---

## 셋 · 작업 규칙 (세 번 깨뜨리고 배운 것)

- **JS 는 Edit 도구로 고친다.** 파이썬 문자열 패치를 bash 히어독으로 태우면
  `'\n'` 이 진짜 개행이 되어 문자열 리터럴이 쪼개진다. 세 번 깨졌다.
- 저장소는 **CRLF**. 파이썬으로 만질 땐 읽고 `\r\n`→`\n`, 쓸 때 되돌린다.
- **로컬 개발 서버를 쓴다** — `preview_start {name:"chaeksa-app"}` (포트 8791).
  밀어놓고 30~60초 기다리지 마라. 배포 전에 콘솔에서 잡을 수 있다.
- 배포는 `python tools_bust.py --bump` → commit → push → GitHub Pages(30~60초).
  **새 JS 파일은 `tools_bust.py` 의 FILES 에 넣어야** `?v=` 가 오른다.
  가드가 있다 — 따옴표 안 생개행이 있으면 bump 를 거부한다.
- 측정 전에는 **서비스워커 해제 + 캐시 삭제**부터. 안 그러면 옛 엔진을 재고 「전부 통과」를 보고하게 된다.
- 워크플로 서브에이전트 스키마 키는 **ASCII 만**(`^[a-zA-Z0-9_.-]{1,64}$`). 한글 키로 34개가 죽었다.
- **이모지를 화면에 쓰지 않는다.** 인장 한자를 쓴다(공주님 원칙 기각 목록).

---

## 넷 · 하지 않기로 한 것

책사별 친밀도·적중률 집계(판정이 인기순으로 왜곡된다) · 적중률 숫자(재 보지 않은 것을
재었다고 말하지 않는다) · 판정 유료화(폐기) · 새 콘텐츠 더하기(해상도가 먼저) · `service_role`.

---

## 다섯 · 일하는 방식

- **전적 위임이다.** 결정해서 실행하고 보고한다. 질문으로 막지 않는다.
  **배포(main push)도 묻지 않는다** — 검증했으면 바로 민다(2026-09-03 「미는 걸 왜 자꾸 물어보냐」).
  먼저 알리는 것은 되돌릴 수 없는 일뿐이다: 삭제 · 바깥 발행(블로그·결제) · 되돌릴 수 없는 마이그레이션.
- **사례를 받으면 구현 전에 조문으로 되받아쓴다**(지식 전수 프로토콜). 법전은 `docs/19`.
- 상태·다음 할 일은 `docs/` 가 아니라 **Claude 메모리**에 있다. 이어서 할 때 거기부터 읽어라.
