# 궁극의 책사

이 파일은 **매 세션 자동으로 로드된다**(서브에이전트에도 간다).
2026-09-20 사장님 「법전, 메모리 피드백 항목 제외하고 다 삭제해」 — 여기 있던 선(판정과 말 · 돈 · 비밀) · 「하지 않기로 한 것」 · 작업 금지 조항을 지웠다.
지키는 것은 둘이다: **법전(`docs/19_법전.md`)** 과 **Claude 메모리의 피드백 항목**.
아래는 규칙이 아니라 길잡이와 작업 방법이다.

---

## 콘텐츠는 네 축으로 만든다 (2026-09-27 사장님 — 사실단, 원효 · 부처의 방법)

모든 화면 · 원고 · 연재는 이 네 칸, 이 차례로 짠다. 나도 사장님도 이걸 기준으로 본다. 전문 `docs/90_사실단_설계축.md`.
1. **세계실단** — 세속의 상식과 언어로: 「하려던 말」 한 줄(태어난 날의 글자 · 나를 뜻하는 글자 · 상사나 고객).
2. **대치실단** — 이 상황의 해결책: 그래서 무엇을 할지, **결정까지 말한다.**
3. **각각위인실단** — 듣는 사람에 따라: 왜 당신에게 그런지, 당신 글자로.
4. **제일의실단** — 진리를 대놓고: 판정 · 고전 원문 그대로(근거 펼침).

## 어디에 무엇이 있나

| 만지는 것 | 문서 |
|---|---|
| 명리 판정·통변 문장 | `docs/19_법전.md` |
| 엔진의 표·상수·산식 | `docs/15_동결표.md` (948줄. 해당 절만 열어도 된다) |
| 격국 성패 | `docs/16_자평진전_성패.md` |
| 조후·온도 | `docs/12_조후용신표.md` |
| 재물 그릇 | `docs/14_재물그릇_산식.md` |
| 재·관을 삶으로 읽는 법 | `docs/27_영역관제.md` |
| 영역을 무엇으로 말할까 | `docs/28_영역전개_전략.md` |
| 형·충·파·해 | `docs/52_형충파해_원문채록.md` |
| 적천수(기세 · 원류 · 통관) | `docs/55_적천수_원문채록.md` |
| 인연·궁합 | `docs/18_인연법.md` · `docs/23_도화홍염_원문.md` · `docs/32_속궁합_원문.md` |
| 주변인 | `docs/30_주변인.md` |
| 홈·화면 구조 | `docs/29_비서.md` |
| 화자·책사 캐릭터 | `docs/22_책사단.md` |
| 새 화면·새 문장의 값어치 | `docs/24_왜_보는가.md` |
| 무엇을 먼저 할 것인가 | `docs/25_전략.md` |
| 블로그·자료 형식 | `docs/10_두형식.md` |
| 결제 | `docs/17_결제.md` |

**부딪히면 법전(19)이 이긴다.**

죽은 문서 — `docs/00_현재상태.md` · `docs/20_협기변방서.md` · `docs/19_법전_원본_2026-09-04.md`. 근거로 쓰지 않는다.

---

## 작업 방법

- 저장소는 **CRLF**. 파이썬으로 만질 땐 읽고 `\r\n`→`\n`, 쓸 때 되돌린다.
- 로컬 개발 서버 — `preview_start {name:"chaeksa-app"}` (포트 8791).
- 배포 — `python tools_bust.py --bump` → commit → push → GitHub Pages(30~60초). 새 JS 파일은 `tools_bust.py` 의 FILES 에 넣어야 `?v=` 가 오른다.
- 측정 전에는 서비스워커 해제 + 캐시 삭제부터(옛 엔진을 재게 된다).
- 워크플로 서브에이전트 스키마 키는 ASCII 만(`^[a-zA-Z0-9_.-]{1,64}$`).
- 월별 글 — 개발 서버에서 달을 덤프 → `tools_wolbyeol.py`(종합) · `tools_gungtong.py`(궁통보감) → `tools_jeongbon.py`(정본).

---

## 일하는 방식

- **전적 위임이다.** 결정해서 실행하고 보고한다. 배포(main push)도 묻지 않는다.
- **먼저 추론하고, 사장님은 검수한다**(2026-09-25). 사장님이 준 큰 원칙과 옛 책 이치로 끝까지 추론해 원고 · 엔진을 먼저 내놓는다. 조문을 기다리며 「어떻게 읽나요?」로 멈추지 않는다. 사장님은 틀린 곳만 짚고, 법전에는 사장님이 고친 것만 올린다.
- 상태·다음 할 일은 `docs/` 가 아니라 **Claude 메모리**에 있다. 이어서 할 때 거기부터 읽는다.
- **사장님이 정하거나 고친 것은 그 턴에 저장한다**(2026-09-25). 저장하지 않은 결정은 세션이 길어지거나 바뀌면 사라진다(「책사 이름 쓰지 말자」를 잊고 다시 꺼낸 일). 세 곳에: ① Claude 메모리 feedback/project 메모 ② 그 일을 맡는 에이전트 파일(`.claude/agents/chaeksa-*` — **지금 규칙만, 옛 줄은 지우고 고쳐 쓴다. 덧붙이기 금지**) ③ 작업판. 보고 끝에 「저장: …」 한 줄. 사장님이 「저장해」라고 하지 않아도 한다.
- **내놓기 전에 사장님 질문을 먼저 던진다**(2026-09-25 「결국 내가 하나하나 손봐야 한다는 건데」). 늘어나나 · 더 넓게 추론했나 · 계산할 걸 쓰고 있나 · 되풀이 · 정체에 맞나 · 거부감 · 도구 문제 · 먼저 저장 — 걸리면 고치고, 보고에 「미리 던진 질문 · 내 답」.
- **새 개인화 콘텐츠는 스킬 `개인화콘텐츠`부터**(2026-09-25 연애궁합 웹툰 완성에서). 뼈대 + 조각 · 계산할 건 엔진 · 작가 → 검수 · 빈칸 0 시험. 전문 docs/76.
- **원고는 메인이 쓰지 않는다**(2026-09-25). 화면에 나갈 글은 사장님이 말하지 않아도 `chaeksa-writer` 에이전트가 쓰고 `chaeksa-reviewer` 가 검수한다. 메인은 조립기 · 문장 틀 코드 · 배포.
- **일은 작업판(`docs/작업판.md`)으로 돈다**(2026-09-25). 사장님 말은 받는 즉시 작업판에 적고 담당 에이전트(`.claude/agents/chaeksa-*`)를 붙인다. 원고는 작가 → 검수 → 화면 → 시험·배포. 닫을 땐 증거(커밋 · 시험 수 · 라이브)를 적는다.
