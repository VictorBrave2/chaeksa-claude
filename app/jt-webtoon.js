/* 정통사주 웹툰 조립기 (09-26 — docs/80 승격안 · docs/81_v2 뼈대 · docs/82 갈림 · docs/83 조각표).
 * 한 사람의 명식 → 20회. 한 회 = 컷 셋 · 한 컷 한 줄: 책사(물음) · 장면 · 나 · 답 + 접힘(해설 · 단서).
 * 갈림: 격 10(언행 — 3 · 4 · 11 · 15 · 20회) · 성패 4(상태 — 6 · 20회) — 곱하지 않는다. 나머지 회는 공통 장면 + 엔진 값으로 채운 답 틀.
 * 조각 · 공통 · 답 틀은 jt-webtoon-2.js(작가). 이 파일에는 원고가 없다 — 사람() · 뼈대 · 조립()만.
 * 성패는 성격 · 기신 · 구응 · 파격 넷으로만(판정키 섰다 · 띠었다 · 구제됐다 · 깨졌다 → 넷). 건록 → 비견 · 양인 → 겁재. */
(function (global) {
  const E = global.ChaeksaEngine;
  const 오행ko = ['나무', '불', '흙', '쇠', '물'];
  const 계절of = (br) => ['겨울', '겨울', '봄', '봄', '봄', '여름', '여름', '여름', '가을', '가을', '가을', '겨울'][br];   // 子丑 겨울 · 寅卯辰 봄 · 巳午未 여름 · 申酉戌 가을 · 亥 겨울
  const 성패키 = { 섰다: '성격', 띠었다: '기신', 구제됐다: '구응', 깨졌다: '파격' };
  const 격정리 = (g) => ({ 건록: '비견', 양인: '겁재' })[g] || g;
  // 십신 → 화면 말(답 · 장면에 사주 말을 안 내려고). 회사 다섯 칸 말(지시 · 자료 · 성과 · 실행 · 동료)도 피한다.
  const 십신말 = { 비견: '나와 같은 편', 겁재: '나와 같은 편', 식신: '내 재주', 상관: '내 재주', 편재: '돈', 정재: '돈', 편관: '맡겨지는 일', 정관: '맡겨지는 일', 편인: '배움', 정인: '배움' };
  const 무리말 = { 비겁: '나와 같은 편', 식상: '내 재주', 재성: '돈', 관성: '맡겨지는 일', 인성: '배움' };
  const 상신말 = (s) => String(s || '').replace(/재성/g, '돈').replace(/인성/g, '배움').replace(/식상|식신|상관/g, '내 재주').replace(/관성|정관|편관|관살/g, '맡겨지는 일').replace(/비겁|비견|겁재/g, '나와 같은 편').replace(/과/g, '과');

  // 격 → 삶의 말(답에 낼 수 있는 꼴, 비유 열쇠말 없이)
  const 격말 = { 정관: '맡은 일을 반듯하게 지키는 쪽', 편관: '힘든 일을 버텨 내는 쪽', 정재: '꼬박꼬박 챙기는 쪽', 편재: '크게 벌여 굴리는 쪽', 정인: '배우는 쪽', 편인: '혼자 파고드는 쪽', 식신: '만들어 내는 쪽', 상관: '틀을 깨고 말하는 쪽', 비견: '옆 사람과 나란히 가는 쪽', 겁재: '겨루며 가는 쪽' };
  function 사람(R, today) {
    const P = global.ChaeksaPanjeong, T = global.ChaeksaTypecard, S = global.ChaeksaSamyeong;
    const p = R.pillars, ds = p.day.stem, 판 = P.판정(R, today || new Date());
    const 층들 = 판.층들 || [], 원 = 층들[0] || {};
    // 건록 · 양인은 판정엔진이 격 층을 안 내고 층.성패(typecard)만 낸다 — 09-26부터 운 층마다도 성패를 내니(panjeong) 대운도 잰다. 격표도 못 낸 원국만 못 잼
    const 원격 = (원.격 && 원.격.성패) || 원.성패 || T.gyeok(R), 대운잼 = !!((원.격 && 원.격.지금격) || 원.성패);
    const 격 = 격정리(원격.격 || 원격.name), 성패 = 성패키[원격.판정] || '성격';
    const 잰 = Object.assign({}, 원격.잰것 || {});
    { const 격무리 = ({ 비견: '비겁', 겁재: '비겁', 식신: '식상', 상관: '식상', 편재: '재성', 정재: '재성', 편관: '관성', 정관: '관성', 편인: '인성', 정인: '인성' })[격]; if (격무리 && !(잰[격무리] || 0)) 잰[격무리] = 0.01; }   // 격 자체(월지)는 있는 힘(검수 2바퀴)
    const 있는힘 = Object.keys(무리말).filter(k => (잰[k] || 0) > 0).map(k => 무리말[k]), 없는힘 = Object.keys(무리말).filter(k => !(잰[k] || 0)).map(k => 무리말[k]);
    // 09-26 검수: 기신은 판정 근거 문장에서 십신 낱말을 뽑는다(엔진이 글자를 따로 안 넘김). 구응은 판정이 구응일 때만. 상신이 비면 격표 상신.
    // 「관살이 없어 …」 꼴의 십신만 걷고(빈자리), 「막아 줄 글자가 없어요」 같은 문장은 살린다(되묻기 2바퀴 ⑥)
    const 근거글 = ((원격.근거 || {}).깨졌다 || []).concat((원격.근거 || {}).띠었다 || []).join(' ').replace(/(비견|겁재|식신|상관|편재|정재|편관|정관|편인|정인|관살|재성|인성|식상|비겁)(이|가)?\s*없[어다고]/g, ' ');
    const 기신십신 = (근거글.match(/비견|겁재|식신|상관|편재|정재|편관|정관|편인|정인|관살|재성|인성|식상|비겁/) || [])[0] || '';
    const 기신 = 기신십신, 구응 = 성패 === '구응';
    // 상신: 엔진이 비면 격표 상신을 쓰되, 그 무리가 원국에 없으면(잰것 0) 비운다 — 「앉힌 손이 없다」 갈래로(검수 2바퀴)
    const 무리키 = (t) => /재/.test(t) ? '재성' : /인/.test(t) ? '인성' : /식|상/.test(t) ? '식상' : /관|살/.test(t) ? '관성' : /비|겁/.test(t) ? '비겁' : '';
    const 격표상신 = (T.gyeok(R) || {}).상신 || '';
    const 상신원 = 원격.상신 || (격표상신 && String(격표상신).split(/과|와|·/).map(x => x.trim()).filter(Boolean).every(x => (잰[무리키(x)] || 0) > 0) ? 격표상신 : '');
    // 대운마다 격 · 성패(실제 연도) — 격이 바뀌는 해 · 돌아오는 해
    const 대운 = ((R.daeun && R.daeun.list) || []).map(d => { let g = 격, s = 성패; try { const j = P.판정(R, new Date(d.startYear + 1, 6, 1)); const l = (j.층들 || []).find(x => x.name === '대운' || x.이름 === '대운') || (j.층들 || [])[1]; const sp = (l && l.격 && l.격.성패) || (l && l.성패); if (sp) { g = 격정리(sp.격); s = 성패키[sp.판정] || s; } } catch (e) {} return { 시작: d.startYear, 끝: d.startYear + 9, 간지: E.STEMS[d.stem] + E.BRANCHES[d.branch], 격: g, 성패: s }; });
    const 올해 = (today || new Date()).getFullYear(), 지금대운 = 대운.find(d => 올해 >= d.시작 && 올해 <= d.끝) || null, 지금i = 대운.indexOf(지금대운);
    const 태어난해 = R.pillars && R.input ? R.input.year : (R.birth && R.birth.year) || (대운[0] ? 대운[0].시작 - (R.daeun.list[0].startAge || 0) : 0);
    const 너무늦음 = (d) => !!(d && 태어난해 && d.시작 - 태어난해 > 70);   // 70세 넘어 오는 해는 「없음」으로(검수 2바퀴 — 「잠깐 접힌」 뒤에 60년이 붙던 것)
    대운.forEach(d => { d.늦음 = 너무늦음(d); });
    const 펴짐0 = (성패 === '파격' || 성패 === '기신') ? (대운.slice(Math.max(0, 지금i)).find(d => d.성패 === '성격' || d.성패 === '구응') || null) : null, 펴짐 = 펴짐0 && !펴짐0.늦음 ? 펴짐0 : null;   // 원국이 접힌 사람이 펴지는 첫 열 해
    const 바뀜전부 = 대운.filter(d => d.격 !== 격 && !d.늦음 && d.시작 - 태어난해 >= 20), 올해0 = (today || new Date()).getFullYear();   // 스무 살 전 바뀜은 안 냄(3바퀴: 12살에 돈 · 일이 달라졌다?)
    // 되묻기 2바퀴 ③: 지난 열 해의 바뀜을 앞으로 올 일처럼 말하지 않게 — {바뀐해}는 지금 · 앞으로, {지난바뀐해} · {지난돌아온해}는 이미 지난 것
    const 바뀜 = 바뀜전부.filter(d => d.끝 >= 올해0), 지난바뀜 = 바뀜전부.filter(d => d.끝 < 올해0);
    const 지난돌아옴 = 지난바뀜.length ? (대운.slice(대운.indexOf(지난바뀜[지난바뀜.length - 1]) + 1).find(d => d.격 === 격) || null) : null;
    // 돌아옴: 지금 격이 바뀌어 있으면 다음에 원격으로 오는 열 해, 지금은 원격이어도 앞으로 바뀜이 있으면 그 뒤에 돌아오는 열 해(3바퀴 E19)
    const 돌아옴0 = 지금대운 && 지금대운.격 !== 격 ? (대운.slice(지금i + 1).find(d => d.격 === 격) || null)
      : (바뀜.length ? (대운.slice(대운.indexOf(바뀜[0]) + 1).find(d => d.격 === 격) || null) : null);
    const 돌아옴 = 돌아옴0 && !돌아옴0.늦음 ? 돌아옴0 : null;
    const 운성 = (S.십이운성(R) || []), 일지운성 = (운성.find(x => x.자리 === '일지') || {});
    const 신살 = (S.신살(R) || []).map(x => ({ 이름: x.이름, 뜻: (x.글 || [])[3] || '', 줄: (x.줄 || [])[0] || '' })), 귀인 = (S.귀인(R) || []).map(x => ({ 이름: x.이름, 뜻: (x.글 || [])[3] || '', 줄: (x.줄 || [])[0] || '' }));
    const 십 = (k) => E.TEN_GODS[E.tenGod(ds, p[k].stem)], 자리 = ['year', 'month', 'hour'].filter(k => p[k]);
    const 재성자리 = 자리.filter(k => /정재|편재/.test(십(k))).map(k => ({ year: '태어난 해', month: '태어난 달', hour: '태어난 시' })[k]).concat(/정재|편재/.test(격) ? ['태어난 달'] : []).filter((x, i, arr) => arr.indexOf(x) === i), 재성종류 = 자리.map(십).find(x => /정재|편재/.test(x)) || (/정재|편재/.test(격) ? 격 : '');   // 09-26 작가: /재/ 는 겁재도 잡았다
    // 되묻기 2바퀴 ①④: 있음/없음은 천간 십신 + 격 무리 + 잰것 을 한 셈으로(비견격인데 「비겁 없음」이 나오던 것)
    const 무리있음 = (re, 키) => 자리.some(k => re.test(십(k))) || re.test(격) || (잰[키] || 0) > 0;
    const 비겁있음 = 무리있음(/비견|겁재/, '비겁'), 관성있음 = 무리있음(/관/, '관성'), 재성있음 = 무리있음(/재/, '재성'), 인성있음 = 무리있음(/인/, '인성'), 식상있음 = 무리있음(/식신|상관/, '식상');
    const SS = global.ChaeksaSsom, 바람 = SS ? SS.일지십신(R) : '', 언 = SS ? (SS.언행(R) || [])[0] : null, 언행키 = 언 ? 언.십신 + (언.드러남 ? '드러남' : '숨음') : '없음';
    let 조후 = null; try { 조후 = global.ChaeksaClassic.gungtong(R); } catch (e) {}
    return {
      R, 일간: E.STEMS[ds], 일간오행: 오행ko[E.STEM_ELEM[ds]], 일간크기: E.STEM_YANG[ds] ? '큰' : '작은', 계절: 계절of(p.month.branch), 월지: E.BRANCHES[p.month.branch],
      찾는글자: 조후 ? 조후.need : '', 찾는글자있음: !!(조후 && 조후.hasMain), 돕는글자있음: !!(조후 && 조후.hasAux),
      격, 성패, 대운잼, 상신: 상신원, 상신말: 상신말(상신원), 있는힘, 없는힘, 기신, 기신말: 기신 ? (십신말[기신] || 무리말[기신] || 상신말(기신)) : '', 구응: !!구응, 펴짐: 펴짐,
      대운, 지금대운, 다음대운: 대운[지금i + 1] || null, 바뀜, 지난바뀜, 지난돌아옴, 돌아옴, 돌아옴잘림: !!(돌아옴0 && 돌아옴0.늦음), 올해,
      일지운성: 일지운성.단계 || '', 일지운성뜻: 일지운성.뜻 || '', 신살, 귀인,
      재성종류, 재성자리, 비겁있음, 관성있음, 재성있음, 인성있음, 식상있음, 바람, 언행키,
    };
  }

  /* 뼈대 v2 — 20회. 줄: ['책사', 물음] · ['@장면', 회] · ['@나', 회] · ['@답', 회] · ['@해설', 회] · ['@단서', 회].
   * @는 조각 · 공통 · 답 틀에서 채운다(jt-webtoon-2.js). 회 번호가 곧 열쇠. */
  const 격회 = [2, 3, 4, 5, 7, 11, 15, 20], 성패회 = [6, 20];   // 09-26 2 · 5 · 7회 답을 격 갈래로(변수 값을 문장에 안 내려고 — 되묻지 않는 문장)
  const 뼈대 = [
    [1, '태어난 날', '나는 어느 계절 사람으로 태어났을까요?'], [2, '태어난 날', '내 안에선 누가 누굴 밀고 당기고 있을까요?'],
    [3, '회사로 치면', '회사로 치면 나는 어느 자리에 앉은 사람일까요?'], [4, '회사로 치면', '거기 앉은 내가 잘하는 건, 자꾸 걸려 넘어지는 건 뭘까요?'], [5, '회사로 치면', '나를 거기 앉혀 준 건 누구일까요?'],
    [6, '흔들리나', '내가 앉은 곳, 지금 든든한가요 흔들리나요?'], [7, '흔들리나', '흔들린다면 누가 흔들고, 누가 붙잡아 줄까요?'],
    [8, '오늘 하루', '나는 하루를 어떤 힘으로 버티고 있을까요?'], [9, '오늘 하루', '내 옆에 붙어 다니는 것과, 급할 때 올 사람은 누굴까요?'],
    [10, '돈', '돈은 나한테 어느 쪽으로 들어올까요?'], [11, '돈', '들어온 돈, 내 손에 남을까요 새어 나갈까요?'], [12, '돈', '돈이 흔들리는 해는 언제일까요?'],
    [13, '마음', '나는 어떤 사람한테 마음이 갈까요?'], [14, '마음', '좋아지면 나는 어떤 사람이 될까요?'],
    [15, '일', '나는 어떤 일을 할 때 힘이 날까요?'], [16, '일', '나는 혼자일 때 세질까요, 같이일 때 세질까요?'], [17, '일', '일이 바뀌는 해는 언제일까요?'],
    [18, '10년', '지금 나는 어떤 10년을 지나고, 무엇으로 바뀌었을까요?'], [19, '10년', '원래의 나로는 언제 돌아올까요?'], [20, '10년', '그래서 지금 나는 어디쯤 와 있을까요?'],   // 09-26 사장님 「열 해라는 말은 한국인은 안 써」
  ].map(([번호, 묶음, 물음]) => ({ 번호, 묶음, 물음, 줄: [['책사', 물음], ['@장면', 번호], ['@나', 번호], ['@답', 번호], ['@해설', 번호], ['@단서', 번호]] }));

  // 답 틀의 {변수}를 사람 값으로. 없는 변수는 빈 글자(검수 때 드러나게 opts.검수면 {이름} 그대로).
  // 09-26 검수 뒤: {이름} · {이름|조사}(과 · 이라 · 은 · 이 · 을 · 로 — 받침 따라) · {?이름:있을 때 글|없을 때 글}(있을 때 글 안의 {이름}도 채움). 검수 모드면 빈 값을 {이름}으로 남긴다.
  const 받침 = (t) => { const c = String(t).charCodeAt(String(t).length - 1) - 0xAC00; return c >= 0 && c < 11172 ? (c % 28) !== 0 : /[0-9]/.test(String(t).slice(-1)) ? /[013678]/.test(String(t).slice(-1)) : false; };
  const 조사 = { 과: ['과', '와'], 이라: ['이라', '라'], 은: ['은', '는'], 이: ['이', '가'], 을: ['을', '를'], 로: ['으로', '로'], 이에요: ['이에요', '예요'] };
  function 채우기(틀, 사, opts) {
    let t = String(틀 || '');
    // {?이름:A|B} — A · B 안에 {이름}이 들어 있어도 되게 중괄호를 세며 짝을 찾는다
    for (let guard = 0; guard < 50; guard++) {
      const i = t.indexOf('{?'); if (i < 0) break;
      let depth = 0, j = i, bar = -1;
      for (; j < t.length; j++) { const c = t[j]; if (c === '{') depth++; else if (c === '}') { depth--; if (depth === 0) break; } else if (c === '|' && depth === 1 && bar < 0) bar = j; }
      if (j >= t.length) break;
      const colon = t.indexOf(':', i), k = t.slice(i + 2, colon), A = bar < 0 ? t.slice(colon + 1, j) : t.slice(colon + 1, bar), B = bar < 0 ? '' : t.slice(bar + 1, j);
      const v = 값(k, 사); t = t.slice(0, i) + ((v == null || v === '') ? B : A) + t.slice(j + 1);
    }
    return t.replace(/\{([^}|]+)(?:\|([^}]+))?\}/g, (m, k, j) => {
      const v = 값(k, 사); if (v == null || v === '') return opts && opts.검수 ? '{' + k + '}' : '';
      if (j && 조사[j]) { const pair = 조사[j]; return String(v) + (받침(v) ? pair[0] : pair[1]); }
      return String(v);
    });
  }

  function 값(k, 사) {
    switch (k) {
      case '계절': return 사.계절; case '오행': return 사.일간오행; case '크기': return 사.일간크기; case '월지': return 사.월지;
      case '찾는글자있음': return 사.찾는글자있음 ? '있어요' : '';
      case '있는힘': return 사.있는힘.join(' · '); case '없는힘': return 사.없는힘.join(' · ');
      case '상신말': return 사.상신말; case '기신말': return 사.기신말; case '구응': return 사.구응 ? '있어요' : '';
      case '운성': return 사.일지운성; case '운성뜻': return 사.일지운성뜻;
      case '신살': return 사.신살.map(x => x.이름).join(' · ') || '없음'; case '신살뜻': return (사.신살[0] || {}).뜻 || '';
      case '귀인': return 사.귀인.map(x => x.이름).join(' · ') || '없음'; case '귀인뜻': return (사.귀인[0] || {}).뜻 || '';
      case '재성자리': return 사.재성자리.map((x, i, a) => i === a.length - 1 ? x : x + (/[가-힣]$/.test(x) && (x.charCodeAt(x.length - 1) - 0xAC00) % 28 ? '과 ' : '와 ')).join(''); case '재성종류': return 사.재성종류 === '정재' ? '꼬박꼬박' : 사.재성종류 === '편재' ? '한꺼번에' : '';
      case '비겁': return 사.비겁있음 ? '있어요' : ''; case '관성': return 사.관성있음 ? '있어요' : '';   // 없으면 빈 값 — {?이름:…|…} 갈래용
      case '지금시작': return 사.지금대운 ? 사.지금대운.시작 : ''; case '지금끝': return 사.지금대운 ? 사.지금대운.끝 : '';
      case '지금격': return 사.지금대운 ? 사.지금대운.격 : 사.격; case '지금성패': return 사.지금대운 ? 사.지금대운.성패 : 사.성패;
      case '바뀐해': return 사.바뀜.length ? 사.바뀜[0].시작 : ''; case '돌아오는해': return 사.돌아옴 ? 사.돌아옴.시작 : '';
      case '지난바뀐해': return 사.지난바뀜.length ? 사.지난바뀜[0].시작 : ''; case '지난돌아온해': return 사.지난돌아옴 && 사.지난돌아옴.시작 <= 사.올해 ? 사.지난돌아옴.시작 : '';
      case '다음시작': return 사.다음대운 ? 사.다음대운.시작 : ''; case '올해': return 사.올해;
      case '격': return 사.격; case '성패': return 사.성패;
      case '비겁있음': return 사.비겁있음 ? '있음' : ''; case '관성있음': return 사.관성있음 ? '있음' : ''; case '재성있음': return 사.재성있음 ? '있음' : ''; case '인성있음': return 사.인성있음 ? '있음' : ''; case '식상있음': return 사.식상있음 ? '있음' : '';
      case '흔들림': return (사.성패 === '파격' || 사.성패 === '기신' || 사.성패 === '구응') ? '있음' : '';   // 구응 = 흔들렸다가 막힘(2바퀴 ②)   // 기신 글자를 못 뽑아도(「막을 글자가 없다」 꼴) 흔들리는 사람은 흔들린다 — 7회 「없어요」 갈래는 이걸로
      case '격말': return 격말[사.격] || ''; case '지금격말': return 격말[사.지금대운 ? 사.지금대운.격 : 사.격] || '';   // 2바퀴 ⑤ — 18회 「무엇으로 바뀌었나」
      case '펴지는중': return 사.펴짐 && 사.펴짐.시작 <= 사.올해 ? '중' : '';   // 펴지는 해가 이미 왔으면(3바퀴 A6 「2024년부터 풀려요」 → 「풀리고 있어요」)
      case '지금흔들림': return 사.지금대운 && (사.지금대운.성패 === '파격' || 사.지금대운.성패 === '기신') ? '있음' : '';   // 6회는 「지금」을 묻는다(3바퀴 E6)
      case '돌아옴흔들림': return 사.돌아옴 && /파격|기신/.test(사.돌아옴.성패) ? '있음' : '';   // 돌아오는 10년이 흔들리는 상태(4바퀴 C19)
      case '돌아옴잘림': return 사.돌아옴잘림 ? '있음' : '';   // 70세 뒤라 잘렸을 뿐 원격 대운이 있음(4바퀴 A19 「안 돌아와요」 금지)
      case '지난돌아옴지금': return 사.지난돌아옴 && 사.지금대운 && 사.지난돌아옴.시작 === 사.지금대운.시작 ? '있음' : '';   // 돌아온 10년이 곧 지금(4바퀴 E19)
      case '바뀜지남': return 사.바뀜.length && 사.바뀜[0].시작 <= 사.올해 ? '있음' : '';   // 바뀐 해가 이미 왔음(「부터 이미」)
      case '바뀌는중': return (사.지금대운 && 사.지금대운.격 !== 사.격) ? '중' : '';   // 지금 10년이 이미 바뀐 기운(작가 요청 — 「이미 바뀌어 있어요」 갈래)
      case '대운잼': return 사.대운잼 ? '잼' : '';   // 건록 · 양인은 빈 값 → {?대운잼:…|열 해마다 어떻게 바뀌나는 아직 못 재요}
      case '펴지는해': return 사.펴짐 ? 사.펴짐.시작 : ''; case '같은격': return 사.지금대운 && 사.지금대운.격 === 사.격 ? '같음' : (사.지금대운 ? '' : '같음');
      case '찾는글자': return 사.찾는글자; case '지금간지': return 사.지금대운 ? 사.지금대운.간지 : '';
      case '운성말': return ((global.ChaeksaJtJogak || {}).운성말 || {})[사.일지운성] || '';
      case '신살말': return 사.신살.map(x => ((global.ChaeksaJtJogak || {}).신살말 || {})[x.이름]).filter(Boolean).slice(0, 1).join(', ') || '';   // 답 34자 한도 — 하나만(나머지는 단서 {신살})
      case '귀인말': return 사.귀인.map(x => ((global.ChaeksaJtJogak || {}).귀인말 || {})[x.이름]).filter(Boolean).slice(0, 1).join(', ') || '';
      case '언행': return ({ 식신드러남: '챙기는 게 바로 보이는 사람', 식신숨음: '말없이 챙겨 놓는 사람', 상관드러남: '할 말이 먼저 나오는 사람', 상관숨음: '할 말을 속에 두는 사람', 없음: '겉으로는 티가 잘 안 나는 사람' })[사.언행키] || ''; case '바람': return (global.ChaeksaSsom && global.ChaeksaSsom.바라는사람 || {})[사.바람] || '';
      default: return null;
    }
  }

  function 조립(R, opts) {
    opts = opts || {};
    const J = global.ChaeksaJtJogak || {}, 사 = 사람(R, opts.오늘);
    const 빈 = (종, 회) => ['장면', '(' + 회 + '회 ' + 종 + ' 조각은 쓰고 있어요)'];
    return 뼈대.map(h => {
      const 회 = h.번호, out = [];
      const 갈래 = 격회.indexOf(회) >= 0 ? (J.격 && J.격[회] && J.격[회][사.격]) : null;
      const 상태 = 성패회.indexOf(회) >= 0 ? (J.성패 && J.성패[회] && J.성패[회][사.성패]) : null;
      const 공통 = J.공통 && J.공통[회];
      h.줄.forEach(([종, a]) => {
        if (종[0] !== '@') { out.push([종, a]); return; }
        const 키 = 종.slice(1);
        let 말 = null;
        if (갈래 && 갈래[키] != null) 말 = 갈래[키];
        else if (상태 && 상태[키] != null) 말 = 상태[키];
        else if (공통 && 공통[키] != null) 말 = 공통[키];
        if (키 === '단서' && 말 == null && J.단서틀 && J.단서틀[회]) 말 = J.단서틀[회];
        if (말 == null) { if (opts.검수) out.push(빈(키, 회)); return; }
        out.push([키, 채우기(말, 사, opts)]);
      });
      return { 번호: 회, 묶음: h.묶음, 물음: h.물음, 줄: out };
    });
  }
  global.ChaeksaJtWebtoon = { 사람, 뼈대, 조립, 격회, 성패회, 채우기, 격말 };
})(window);
